@model IEnumerable<Day3MVC.Models.ZipToCities>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.City.CityName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zip.ZipCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.City.CityName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zip.ZipCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ZToC }) |
            @Html.ActionLink("Details", "Details", new { id=item.ZToC }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ZToC })
        </td>
    </tr>
}

</table>
